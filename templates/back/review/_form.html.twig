{% set isNew = (app.request.attributes.get("_route") ends with "review_new")%}

<div class="w-11/12 md:w-1/3 mx-2 md:mx-auto flex flex-col justify-center">

    {{ form_start(form) }}

        {{ form_widget(form) }}
        
        <button class="flex w-full justify-center rounded-md bg-cyan-900 px-3 py-1.5 text-base font-medium leading-6 text-white shadow-sm hover:bg-sky-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-gray-600 mt-6">{{ button_label|default('Sauvegarder') }}</button>

    {{ form_end(form) }}
    <div class="flex flex-col"> 
        <a href="{{ path('app_back_review_index') }}">
            <button type="button" class="w-full  my-1 justify-center relative overflow-hidden inline-flex items-center border-2 border-blue-400 focus:outline-none text-gray-900 rounded-md group bg-gradient-to-br from-cyan-900 to-blue-500 group-hover:from-cyan-500 group-hover:to-blue-500 hover:text-white focus:ring-cyan-200">
                <span class="relative w-full px-3 py-1.5 transition-all ease-in duration-75 bg-white rounded-md group-hover:bg-opacity-0">Retour à la liste</span>
            </button>
        </a>
    </div>
</div>